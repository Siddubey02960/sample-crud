const addUserValidation = require('./add-user-validation');
const getUserValidation = require('./get-user-validation');
const updateUserValidation = require('./update-user-validation');

module.exports = {
    addUserValidation,
    getUserValidation,
    updateUserValidation,
}